<%= semantic_form_for @loan do |f| %>
  <%= f.inputs do %>
    <%= f.input :user, :member_label => :full_name  %>
    <%= f.input :bookwork, :member_label => :title %>
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit, :as => :input %>
  <% end %>
<% end %>
